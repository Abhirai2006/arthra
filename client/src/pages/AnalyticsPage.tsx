import { useFinanceWorkspace } from "@/hooks/useFinanceWorkspace";
import { formatInrFromPaise } from "@shared/finance";
import { deterministicInsightProvider } from "@shared/insights";
import { trpc } from "@/lib/trpc";
import { motion } from "framer-motion";
import { BarChart3, BrainCircuit, Info, Sparkles } from "lucide-react";
import { useMemo, useState } from "react";
import { Area, AreaChart, Bar, BarChart, Cell, Legend, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

const palette = ["#8d74f4", "#eb8b7b", "#63bfc8", "#deb752", "#dd6da2", "#5ab78b"];
const money = (value: number) => formatInrFromPaise(value, 0);

export default function AnalyticsPage() {
  const { activeSpace, activeSpaceId, isLoading } = useFinanceWorkspace(); const [explaining, setExplaining] = useState(false);
  const analytics = trpc.finance.analytics.get.useQuery({ spaceId: activeSpaceId ?? 0 }, { enabled: !!activeSpaceId });
  const insight = useMemo(() => deterministicInsightProvider({ categories: analytics.data?.categoryBreakdown ?? [], monthlyTrend: analytics.data?.monthlyTrend ?? [] }), [analytics.data]);
  if (isLoading || !activeSpace) return <div className="workspace-page"><div className="skeleton h-9 w-56" /><div className="skeleton mt-7 h-80" /></div>;
  const trend = analytics.data?.monthlyTrend ?? []; const categories = analytics.data?.categoryBreakdown ?? []; const unusual = analytics.data?.transactions.filter(item => item.isUnusual) ?? [];
  return <div className="workspace-page"><header className="workspace-head"><div><p className="workspace-kicker">Six-month reflection</p><h1>Analytics, with context.</h1><p className="workspace-subtitle">These views are drawn only from <strong>{activeSpace.name}</strong>. They are directional financial observations, not financial advice.</p></div><button className="workspace-add" type="button" onClick={() => setExplaining(value => !value)}><BrainCircuit size={16} /> Explain the pattern</button></header>{explaining && <motion.article className="explain-card" initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }}><Sparkles size={18} /><div><p className="workspace-kicker">Arthra observation</p><p>{insight}</p></div></motion.article>}<section className="analytics-grid"><motion.article className="workspace-card chart-card chart-card--wide" initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .42 }}><div className="chart-card__head"><div><p className="workspace-kicker">Monthly rhythm</p><h2>Income and spending</h2></div><Info size={16} /></div>{trend.length ? <ResponsiveContainer width="100%" height={260}><AreaChart data={trend} margin={{ top: 12, right: 5, bottom: 0, left: -20 }}><defs><linearGradient id="expenseFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#9e86ff" stopOpacity={.38} /><stop offset="100%" stopColor="#9e86ff" stopOpacity={0} /></linearGradient></defs><XAxis dataKey="monthKey" tick={{ fontSize: 10, fill: "#928a9e" }} axisLine={false} tickLine={false} /><YAxis tickFormatter={value => `₹${Math.round(value / 100)}`} tick={{ fontSize: 10, fill: "#928a9e" }} axisLine={false} tickLine={false} /><Tooltip formatter={(value: number) => money(value)} contentStyle={{ borderRadius: 12, border: "1px solid var(--border)", background: "var(--card)" }} /><Legend iconSize={8} wrapperStyle={{ fontSize: 10 }} /><Area type="monotone" dataKey="expensePaise" name="Spending" stroke="#9e86ff" strokeWidth={2.5} fill="url(#expenseFill)" /><Area type="monotone" dataKey="incomePaise" name="Income" stroke="#5dc09a" strokeWidth={2} fill="transparent" /></AreaChart></ResponsiveContainer> : <ChartEmpty />}</motion.article><motion.article className="workspace-card chart-card" initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .42, delay: .07 }}><div className="chart-card__head"><div><p className="workspace-kicker">Where it goes</p><h2>Category mix</h2></div></div>{categories.length ? <ResponsiveContainer width="100%" height={230}><PieChart><Pie data={categories} dataKey="amountPaise" nameKey="name" innerRadius={60} outerRadius={90} paddingAngle={3}>{categories.map((entry, index) => <Cell key={entry.name} fill={entry.color ? `var(--chart-${(index % 5) + 1})` : palette[index % palette.length]} />)}</Pie><Tooltip formatter={(value: number) => money(value)} contentStyle={{ borderRadius: 12, border: "1px solid var(--border)", background: "var(--card)" }} /></PieChart></ResponsiveContainer> : <ChartEmpty />}</motion.article><motion.article className="workspace-card chart-card chart-card--wide" initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .42, delay: .12 }}><div className="chart-card__head"><div><p className="workspace-kicker">Largest categories</p><h2>Spending breakdown</h2></div><BarChart3 size={16} /></div>{categories.length ? <ResponsiveContainer width="100%" height={260}><BarChart data={categories.slice(0, 6)} layout="vertical" margin={{ top: 3, right: 14, bottom: 3, left: 28 }}><XAxis type="number" tickFormatter={value => `₹${Math.round(value / 100)}`} tick={{ fontSize: 10, fill: "#928a9e" }} axisLine={false} tickLine={false} /><YAxis type="category" dataKey="name" width={90} tick={{ fontSize: 10, fill: "#928a9e" }} axisLine={false} tickLine={false} /><Tooltip formatter={(value: number) => money(value)} contentStyle={{ borderRadius: 12, border: "1px solid var(--border)", background: "var(--card)" }} /><Bar dataKey="amountPaise" radius={[0, 6, 6, 0]}>{categories.slice(0, 6).map((entry, index) => <Cell key={entry.name} fill={palette[index % palette.length]} />)}</Bar></BarChart></ResponsiveContainer> : <ChartEmpty />}</motion.article><motion.article className="workspace-card unusual-card" initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .42, delay: .18 }}><p className="workspace-kicker">Unusual moments</p><h2>{unusual.length ? `${unusual.length} entry${unusual.length === 1 ? "" : "ies"} worth a second look` : "No unusual transactions flagged"}</h2><p>{unusual.length ? "These are simply amounts that are materially higher than recent expenses in this space. They are not errors by default." : "Once there is enough history, Arthra will gently flag expense amounts that sit well outside your recent rhythm."}</p></motion.article></section></div>;
}
function ChartEmpty() { return <div className="chart-empty"><BarChart3 size={24} /><p>There is not enough live data to draw this view yet.</p></div>; }
